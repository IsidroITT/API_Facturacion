const express = require("express");
const router = express.Router();
const mongoose = require("mongoose");
const productDriver = require("../controllers/productsController");


module.exports = router;